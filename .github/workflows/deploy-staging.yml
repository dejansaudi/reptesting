name: Deploy Staging

on:
  push:
    branches: [staging]

jobs:
  deploy:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4

      # ─── Option 1: Railway (uncomment when ready) ───
      # - name: Deploy to Railway
      #   uses: bervProject/railway-deploy@main
      #   with:
      #     railway_token: ${{ secrets.RAILWAY_TOKEN }}
      #     service: validately-staging

      # ─── Option 2: Docker on VM (uncomment when ready) ───
      # - name: Deploy via SSH
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.STAGING_HOST }}
      #     username: ${{ secrets.STAGING_USER }}
      #     key: ${{ secrets.STAGING_SSH_KEY }}
      #     script: |
      #       cd /opt/validately
      #       git pull origin staging
      #       cd 03_PRODUCTION/validately
      #       docker compose up --build -d

      - name: Placeholder
        run: echo "Staging deployment configured. Uncomment your deployment method above."
